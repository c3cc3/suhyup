installation

# yum install autoconf
# yum install automake
# yum install libtool
# yum install ncurses-devel
# yum install cvs
sudo apt install xutils-dev

or

$ sudo apt install cvs
$ sudo apt install autoconf

>>>>>>> 1.3
autoreconf -i
configure --prefix=${install_dir}
ex) configure --prefix=/usr/fq
make
make install

<새로운 서버에서 autoreconf -i 에서 에러가 발생하면 다음과 같이 수행한다> ㅆ竄咀８?파이

error: possibly undefined macro: AM_INIT_AUTOMAKE
..

pi@raspsvr01 ~/L4 $ libtoolize --force

libtoolize: putting auxiliary files in `.'.
libtoolize: linking file `./ltmain.sh'
libtoolize: putting macros in AC_CONFIG_MACRO_DIR, `m4'.
libtoolize: linking file `m4/libtool.m4'
libtoolize: linking file `m4/ltoptions.m4'
libtoolize: linking file `m4/ltsugar.m4'
libtoolize: linking file `m4/ltversion.m4'
libtoolize: linking file `m4/lt~obsolete.m4'
libtoolize: Consider adding `-I m4' to ACLOCAL_AMFLAGS in Makefile.am.
libtoolize: `AC_PROG_RANLIB' is rendered obsolete by `LT_INIT'

pi@raspsvr01 ~/L4 $ aclocal

pi@raspsvr01 ~/L4 $ autoheader

pi@raspsvr01 ~/L4 $ automake --force-missing --add-missing

configure.ac:11: installing `./config.guess'
configure.ac:11: installing `./config.sub'
configure.ac:10: installing `./install-sh'
configure.ac:10: installing `./missing'
src/apps/l4/Makefile.am: installing `./depcomp'

pi@raspsvr01 ~/L4 $ autoconf
pi@raspsvr01 ~/L4 $

<<<<<<< README
# yum install ncurses-devel or # dnf install ncurses-devel or sudo apt-get install libncurses5-dev(Raspi)
=======
# yum install ncurses-devel
# sudo apt-get install libncurses5 libncursesw5-dev

>>>>>>> 1.5
# test

# configure examples
./configure --bindir=/ums/fq/bin --libdir=/ums/fq/lib --includedir=/ums/fq/include

